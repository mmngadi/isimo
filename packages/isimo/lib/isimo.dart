/// Isimo — lightweight, zero-boilerplate, atomic state management for Dart.
///
/// Pure Dart core with zero Flutter dependencies. See the package README for
/// usage. Public API: [createStore], [Store], [StoreState], [persist],
/// [StorageEngine].
library;

export 'src/isimo_base.dart';